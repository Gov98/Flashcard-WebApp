from django.contrib import admin

# Since we're using JSON files and not Django models, there's no need to register models here.
# This is included for completeness in case you switch to Django models in the future.
